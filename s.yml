edition: 1.0.0
access: "default"

services:
  website-starter:
    component: oss
    actions:
      pre-deploy:
        - run: npm run generate
          path: ./
    props:
      bucket: 'alidankun'
      codeUri: ./dist
      acl: public-read
      index: index.html
      region: cn-hangzhou
      # hosts:
      #   - host: auto